apiVersion: v1
kind: Service
metadata:
  name: web
  namespace: waves-demo
  annotations:
    argocd.argoproj.io/sync-wave: "2"
spec:
  selector: { app: web }
  ports:
    - port: 80
      targetPort: 80
  type: ClusterIP
