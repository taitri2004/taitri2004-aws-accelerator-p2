# Terraform apply-on-merge cho cloud/w8/exercise
# Trigger: push lên main, có file dưới cloud/w8/exercise/ thay đổi.
# Gate: Environment "prod" — manual approval trước khi chạy apply.

name: Terraform Apply (w8 exercise)

on:
  push:
    branches: [main]
    paths:
      - 'cloud/w8/exercise/**'
      - '.github/workflows/tf-apply.yml'

permissions:
  contents: read
  id-token: write

env:
  AWS_REGION: ap-southeast-1
  TF_WORKDIR: cloud/w8/exercise/environments/dev

jobs:
  apply:
    runs-on: ubuntu-latest
    environment: prod    # GitHub env → required reviewers gate
    steps:
      - uses: actions/checkout@v4

      - name: Configure AWS via OIDC
        uses: aws-actions/configure-aws-credentials@v4
        with:
          role-to-assume: arn:aws:iam::749043157095:role/gha-tf-apply
          aws-region: ${{ env.AWS_REGION }}

      - uses: hashicorp/setup-terraform@v3
        with:
          terraform_version: 1.15.4

      - name: terraform init
        working-directory: ${{ env.TF_WORKDIR }}
        run: terraform init -input=false

      - name: terraform apply
        working-directory: ${{ env.TF_WORKDIR }}
        run: terraform apply -auto-approve -input=false
